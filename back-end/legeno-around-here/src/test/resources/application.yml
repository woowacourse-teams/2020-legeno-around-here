spring:
  datasource:
    driver-class-name: org.h2.Driver
  jpa:
    properties:
      hibernate:
        format_sql: true

security:
  jwt:
    token:
      secret-key: hihirachsecretkey
      expire_length: 3600000
